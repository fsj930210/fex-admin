# React Avatar Primitive

`AvatarGroup` belongs to the Avatar family. `maxCount` keeps the first items visible and renders `AvatarGroupOverflow` for the remainder. `renderOverflow` customizes the summary without changing the original children.
