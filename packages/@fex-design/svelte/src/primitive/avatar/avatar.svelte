<script lang="ts">import { avatarClassName,type AvatarStyleProps } from '@fex-design/styles/avatar';import { cn } from '@fex/utils';import { setContext,type Snippet } from 'svelte';import { writable } from 'svelte/store';import type { HTMLAttributes } from 'svelte/elements';import { avatarContextKey } from './context';interface Props extends Omit<HTMLAttributes<HTMLSpanElement>,'class'>,AvatarStyleProps{class?:string;children?:Snippet}let{class:className,children,size='md',shape='circle',...rest}:Props=$props();const loaded=writable(false);setContext(avatarContextKey,{loaded})</script>
<span {...rest} data-slot="avatar" data-size={size} data-shape={shape} class={cn(avatarClassName({size,shape}),className)}>{@render children?.()}</span>
