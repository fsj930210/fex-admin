{
  "name": "angular-app",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/angular-app/src",
  "projectType": "application",
  "targets": {
    "dev": {
      "command": "ng serve --project angular-app --host 127.0.0.1 --port 4000",
      "dependsOn": [{ "projects": ["components-angular"], "target": "sync-paths" }],
      "options": { "cwd": "apps/angular-app" }
    },
    "build": {
      "command": "ng build angular-app",
      "options": { "cwd": "apps/angular-app" },
      "outputs": ["{workspaceRoot}/dist/apps/angular-app"]
    },
    "typecheck": { "command": "ngc -p apps/angular-app/tsconfig.json --noEmit" },
    "lint": {
      "command": "oxlint apps/angular-app/src"
    }
  }
}
