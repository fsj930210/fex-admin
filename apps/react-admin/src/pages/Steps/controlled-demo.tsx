import { useState } from 'react'; import { Steps, type StepValue } from '@fex/components-react/primitive/steps'; import { Button } from '@fex/components-react/ui/button'; import { Card } from '@fex/components-react/ui/card'; import { StepList } from './step-list'
export function ControlledDemo(){const [current,setCurrent]=useState<StepValue>('account');return <Card title="Controlled" description="Business completion updates current; navigation requests the same value through onChange."><Steps navigation current={current} onChange={setCurrent}><StepList/></Steps><Button className="mt-space-md" onClick={()=>setCurrent(current==='account'?'profile':'review')}>Complete current task</Button></Card>}
