<script lang="ts">import Steps from '@fex/components-svelte/primitive/steps-root';import Card from '@fex/components-svelte/ui/card';import StepList from './step-list.svelte';let message=$state('Use click, Enter, Space or arrow keys.')</script><Card title="Navigation" description="navigation enables pointer and keyboard switching and reports complete node metadata."><Steps navigation defaultCurrent="account" onchange={(value,meta)=>message=`${String(meta.previous?.value??'none')} → ${String(value)} (${meta.trigger})`}><StepList/></Steps><p class="mt-space-md text-sm text-muted-foreground">{message}</p></Card>
