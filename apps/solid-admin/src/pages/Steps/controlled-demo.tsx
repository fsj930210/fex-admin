import { createSignal } from 'solid-js';import { Steps,type StepValue } from '@fex/components-solid/primitive/steps';import { Button } from '@fex/components-solid/ui/button';import { Card } from '@fex/components-solid/ui/card';import { StepList } from './step-list';export function ControlledDemo(){const [current,setCurrent]=createSignal<StepValue>('account');return <Card title="Controlled" description="Business completion updates current; navigation requests the same value through onChange."><Steps navigation current={current()} onChange={setCurrent}><StepList/></Steps><Button class="mt-space-md" onClick={()=>setCurrent(current()==='account'?'profile':'review')}>Complete current task</Button></Card>}
